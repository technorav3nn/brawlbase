layer: "library"

tasks:
    build:
        deps:
            - "supercell-api-utils:build"
        command: "yarn build"
        inputs:
            - "src/**/*"
            - "tsdown.config.ts"
        outputs:
            - "dist/**/*"
