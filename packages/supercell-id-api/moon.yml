layer: "library"

tasks:
    build:
        command: "yarn build"
        inputs:
            - "src/**/*"
            - "tsdown.config.ts"
            - "package.json"
        outputs:
            - "dist/**/*"
